{
  "name": "related-anime",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "next-frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"yarn workspace @related-anime/server dev\" \"yarn workspace @related-anime/next-frontend dev\"",
    "start": "yarn workspace @related-anime/server start",
    "lint": "concurrently \"yarn lint:root\" \"yarn lint:server\" \"yarn lint:next\"",
    "lint:root": "yarn prettier ./*.* --write --ignore-unknown",
    "lint:server": "yarn workspace @related-anime/server lint",
    "lint:next": "yarn workspace @related-anime/next-frontend lint",
    "prepare": "husky install",
    "setup": "./setup.sh"
  },
  "devDependencies": {
    "concurrently": "^3.6.1",
    "husky": "^7.0.0",
    "prettier": "2.4.1"
  },
  "engines": {
    "node": "16.x"
  }
}
